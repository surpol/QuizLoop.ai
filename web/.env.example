# Optional. Learner reports use local Gemma through Ollama when this is absent.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# SQLite path for the Node deployment.
QUIZLOOP_DB_PATH=web/data/quizloop.sqlite

# Local Gemma runtime.
GEMMA_BASE_URL=http://127.0.0.1:11434
GEMMA_MODEL=gemma4:e2b
