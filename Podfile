platform :ios, '17.0'

target 'Waves' do
  use_frameworks!

  # Google AI Edge / MediaPipe on-device LLM runtime.
  # Install with `pod install`, then open Waves.xcworkspace.
  pod 'MediaPipeTasksGenAI'
  pod 'MediaPipeTasksGenAIC'
end
