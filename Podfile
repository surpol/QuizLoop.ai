platform :ios, '17.0'

target 'QuizLoop' do
  use_frameworks!

  # Google AI Edge / MediaPipe on-device LLM runtime.
  # Install with `pod install`, then open QuizLoop.xcworkspace.
  pod 'MediaPipeTasksGenAI'
  pod 'MediaPipeTasksGenAIC'
end
